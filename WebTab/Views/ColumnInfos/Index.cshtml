@model IEnumerable<WebTab.Models.ColumnInfo>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ColumnOrder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsActive)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ColumnEffect1.Effect)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ColumnOrder)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsActive)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ColumnEffect1.Effect)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ColumnName }) |
                @Html.ActionLink("Details", "Details", new { id=item.ColumnName }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ColumnName })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
